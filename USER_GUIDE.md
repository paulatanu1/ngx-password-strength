# User Guide

The full user guide for `ngx-password-strength-validator` lives at:

**👉 [projects/ngx-password-strength/USER_GUIDE.md](./projects/ngx-password-strength/USER_GUIDE.md)**

(This file is a stub at the repository root so that any tool — including the npmjs.com "Repository" link panel — that auto-resolves `USER_GUIDE.md` at the repo root lands on a valid page instead of a 404. The canonical guide ships inside the npm package itself.)
